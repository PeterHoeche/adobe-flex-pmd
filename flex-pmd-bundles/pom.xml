<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<parent>
		<artifactId>flex-pmd</artifactId>
		<groupId>com.adobe.ac</groupId>
		<version>1.1-SNAPSHOT</version>
	</parent>
	<modelVersion>4.0.0</modelVersion>
	<artifactId>${project.parent.artifactId}-all-in-one-bundle</artifactId>
	<name>Adobe Flex PMD All-in-one bundle</name>
	<packaging>pom</packaging>
	<dependencies>
	
		<!-- FlexPMD -->
	
		<dependency>
			<groupId>${project.parent.groupId}</groupId>
			<artifactId>${project.parent.artifactId}-ant-task</artifactId>
			<version>${project.parent.version}</version>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>${project.parent.groupId}</groupId>
			<artifactId>${project.parent.artifactId}-command-line</artifactId>
			<version>${project.parent.version}</version>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>${project.parent.groupId}</groupId>
			<artifactId>${project.parent.artifactId}-automator</artifactId>
			<version>${project.parent.version}</version>
			<type>pom</type>
			<scope>compile</scope>
		</dependency>

		<!-- FlexCPD -->

		<dependency>
			<groupId>${project.parent.groupId}</groupId>
			<artifactId>${project.parent.artifactId}-cpd-ant-task</artifactId>
			<version>${project.parent.version}</version>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>${project.parent.groupId}</groupId>
			<artifactId>${project.parent.artifactId}-cpd-command-line</artifactId>
			<version>${project.parent.version}</version>
			<scope>compile</scope>
		</dependency>

		<!-- FlexMetrics -->
		
	    <dependency>
			<groupId>${project.parent.groupId}</groupId>
			<artifactId>${project.parent.artifactId}-metrics-ant-task</artifactId>
			<version>${project.parent.version}</version>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>${project.parent.groupId}</groupId>
			<artifactId>${project.parent.artifactId}-metrics-command-line</artifactId>
			<version>${project.parent.version}</version>
			<scope>compile</scope>
		</dependency>

	</dependencies>

	<build>
		<plugins>

			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<version>${maven-antrun-plugin.version}</version>
				<executions>
					<execution>
						<id>package-ant-task</id>
						<phase>install</phase>
						<configuration>
							<tasks>
								<mkdir dir="${project.build.directory}/release" />
								<unzip src="${project.build.directory}/../../${project.parent.artifactId}-ant-task/target/${project.parent.artifactId}-ant-task-${project.parent.version}.zip" dest="${project.build.directory}/release" />
								<unzip src="${project.build.directory}/../../${project.parent.artifactId}-cpd-ant-task/target/${project.parent.artifactId}-cpd-ant-task-${project.parent.version}.zip" dest="${project.build.directory}/release" />
								<unzip src="${project.build.directory}/../../${project.parent.artifactId}-metrics-ant-task/target/${project.parent.artifactId}-metrics-ant-task-${project.parent.version}.zip" dest="${project.build.directory}/release" />
								<unzip src="${project.build.directory}/../../${project.parent.artifactId}-command-line/target/${project.parent.artifactId}-command-line-${project.parent.version}.zip" dest="${project.build.directory}/release" />
								<unzip src="${project.build.directory}/../../${project.parent.artifactId}-cpd-command-line/target/${project.parent.artifactId}-cpd-command-line-${project.parent.version}.zip" dest="${project.build.directory}/release" />
								<unzip src="${project.build.directory}/../../${project.parent.artifactId}-metrics-command-line/target/${project.parent.artifactId}-metrics-command-line-${project.parent.version}.zip" dest="${project.build.directory}/release" />
								<unzip src="${project.build.directory}/../../${project.parent.artifactId}-automator-workflow/target/${project.parent.artifactId}-automator-${project.parent.version}-workflow.zip" dest="${project.build.directory}/release" />
								<delete dir="${project.build.directory}/release/command-line" />
								<zip destfile="${project.build.directory}/${project.build.finalName}.zip" basedir="${project.build.directory}/release" excludes="*.zip" />
							</tasks>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

		</plugins>
	</build>

</project>